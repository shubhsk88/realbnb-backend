type googleSignInResponse {
  ok: Boolean!
  error: String
  token: String
}

type Mutation {
  googleSignIn(
    email: String!
    avatar: String!
    name: String!
    googleId: String!
  ): googleSignInResponse!
}

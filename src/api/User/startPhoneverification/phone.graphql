type phoneVerificationResponse {
  ok: Boolean!
  error: String
  token: String
}

schema {
  query: Query
}

type Query {
  hello: String
}
type Mutation {
  phoneVerification(phoneNumber: String!): phoneVerificationResponse!
}

query Hello {
  hello
}

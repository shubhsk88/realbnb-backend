type phoneVerificationResponse {
  ok: Boolean!
  error: String
  token: String
}

type Query {
  hello: String!
}
type Mutation {
  phoneVerification(phoneNumber: String!): phoneVerificationResponse!
}

query Hello {
  hello
}

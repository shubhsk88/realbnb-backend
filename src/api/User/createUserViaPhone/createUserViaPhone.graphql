scalar DateTime

type createUserViaPhoneResponse {
  ok: Boolean!
  error: String
  token: String
}

type Mutation {
  createUserViaPhone(
    email: String!
    password: String!
    name: String!
    phone: String!
    birthDate: String
  ): createUserViaPhoneResponse!
}
